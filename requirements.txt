# requirements.txt — GitHub Actions CI runner dependencies
# Databricks cluster does NOT use this file — it has its own runtime (DBR 16.4 LTS)

# LangChain stack
langchain==0.3.25
databricks-langchain>=0.1.0        # replaces deprecated langchain-databricks
langchain-community==0.3.24
langchain-core==0.3.59

# MLflow tracing only (lightweight, no databricks-sdk conflict on CI runner)
# Full mlflow is pre-installed on Databricks clusters
mlflow-tracing>=3.1.0

# Other
requests==2.32.3
uvicorn>=0.15.0
